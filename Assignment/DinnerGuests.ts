// Assuming you have an array of guests
const dinerGuests: string[] = ["Guest1", "Guest2", "Guest3", "Guest4"];

// Calculate the number of guests using the length property of the array
const numberOfGuests: number = dinerGuests.length;

// Print a message indicating the number of dinner guests
console.log(`You are inviting ${numberOfGuests} people to dinner.`);
